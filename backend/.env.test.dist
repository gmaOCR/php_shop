# Configuration pour l'environnement de test
# Ce fichier est utilisé automatiquement par PHPUnit
# Copiez vers .env.test si nécessaire (normalement auto-créé)

KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
APP_ENV=test

# Base de données de test (utilise le même serveur MySQL avec suffix _test)
# Doctrine ajoutera automatiquement le suffix via la config test/doctrine.yaml
DATABASE_URL="mysql://shop_user:shop_password@mysql:3306/shop_db?serverVersion=8.0&charset=utf8mb4"
